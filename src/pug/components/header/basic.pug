// #### HEADER ####
header.app-bar.header#header
  .container
    .header-content
      nav.nav-menu.flex-grow-1
        |
        |
        .logo.flex-grow-1.start-mobile
          |
          |
          a(href=link.home)
            include ../branding/logo.pug
            +logo('landscape')
          |
          |
        nav.user-menu
          |
          |
          a.btn.primary.waves-effect(href=basicMenuLink !== undefined ? basicMenuLink : link.login)
            | #{basicMenuText !== undefined ? basicMenuText : $t.login}
          |
          |
          span.spacer.vertical-divider
          include ./top-nav/settings
// #### END HEADER ####